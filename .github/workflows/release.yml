name: Release

on:
  push:
    tags:
      - "v*"
permissions:
  contents: write

jobs:
  build:
    runs-on: windows-latest
    defaults:
      run:
        shell: bash

    steps:
      - uses: actions/checkout@v6
        with:
          fetch-depth: 0 # Для changelog

      - name: Set up Python
        uses: actions/setup-python@v6
        with:
          python-version: "3.12"

      - name: Install PyInstaller
        run: pip install pyinstaller

      - name: Verify version
        run: |
          # Извлекаем версию из main.py
          PY_VERSION=$(grep 'APP_VERSION' main.py | grep -oE '[0-9]+\.[0-9]+\.[0-9]+')
          TAG_VERSION="${GITHUB_REF#refs/tags/v}"

          echo "Версия в main.py: $PY_VERSION"
          echo "Версия тега: $TAG_VERSION"

          if [ "$PY_VERSION" != "$TAG_VERSION" ]; then
            echo "❌ Версии не совпадают!"
            exit 1
          fi

          echo "✅ Версии совпадают"

      - name: Build release
        run: python build.py --release

      - name: Generate changelog
        run: |
          # Получаем предыдущий тег
          git fetch --tags --force
          previous_tag=$(git describe --tags --abbrev=0 HEAD^ 2>/dev/null || echo "")

          if [ -z "$previous_tag" ]; then
            echo "### 🎮 Games List Manager ${{ github.ref_name }}" > changelog.md
            echo "Первый релиз!" >> changelog.md
            echo "" >> changelog.md
            echo "#### Все коммиты:" >> changelog.md
            git log --pretty=format:"- %s" --no-merges >> changelog.md
          else
            echo "### 🎮 Games List Manager ${{ github.ref_name }}" > changelog.md
            echo "" >> changelog.md
            echo "#### Изменения с $previous_tag:" >> changelog.md
            git log --pretty=format:"- %s" --no-merges $previous_tag..HEAD >> changelog.md
          fi

          echo "" >> changelog.md
          echo "---" >> changelog.md
          echo "*Релиз создан автоматически*" >> changelog.md

      - name: Create Release
        uses: softprops/action-gh-release@v2
        with:
          name: Games List Manager ${{ github.ref_name }}
          body_path: changelog.md
          files: dist/*.zip # ТОЛЬКО наш ZIP с приложением
          draft: false
          prerelease: false
